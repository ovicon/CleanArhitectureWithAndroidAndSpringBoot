group 'ro.ovidiuconeac.backend'
version '1.0.0'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    compile("org.springframework.boot:spring-boot-starter-web:1.5.1.RELEASE")
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
